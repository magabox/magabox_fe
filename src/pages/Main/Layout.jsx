import React from "react";
import styled from "styled-components";
const Layout = () => {
	return <LayoutWrap>Layout</LayoutWrap>;
};

export default Layout;

export const LayoutWrap = styled.div`
	width: 1100px;
	margin: 0 auto;
`;
